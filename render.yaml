databases:
  - name: ai-doubt-db-v2
    databaseName: ai_doubt_system
    plan: free
    region: virginia
    ipAllowList:
      - source: 0.0.0.0/0
        description: "Allow connections from anywhere"

services:
  - type: redis
    name: ai-doubt-redis-v2
    plan: free
    region: virginia
    maxmemoryPolicy: noeviction
    ipAllowList:
      - source: 0.0.0.0/0
        description: "Allow connections from anywhere"

  - type: web
    name: ai-doubt-backend-v2
    env: docker
    plan: free
    region: virginia
    dockerContext: backend
    dockerfilePath: backend/Dockerfile
    dockerCommand: sh -c "alembic upgrade head && python seed.py && uvicorn app.main:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: ai-doubt-db-v2
          property: connectionString

      - key: REDIS_URL
        fromService:
          type: redis
          name: ai-doubt-redis-v2
          property: connectionString

      - key: SECRET_KEY
        generateValue: true

      - key: ENVIRONMENT
        value: production

      - key: BACKEND_CORS_ORIGINS
        fromService:
          type: web
          name: ai-doubt-frontend-v2
          property: host

  - type: worker
    name: ai-doubt-worker-v2
    env: docker
    plan: free
    region: virginia
    dockerContext: backend
    dockerfilePath: backend/Dockerfile
    dockerCommand: celery -A app.workers.celery_app.celery_app worker --loglevel=info --pool=solo
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: ai-doubt-db-v2
          property: connectionString

      - key: REDIS_URL
        fromService:
          type: redis
          name: ai-doubt-redis-v2
          property: connectionString

      - key: SECRET_KEY
        fromService:
          type: web
          name: ai-doubt-backend-v2
          envVarKey: SECRET_KEY

      - key: ENVIRONMENT
        value: production

  - type: web
    name: ai-doubt-frontend-v2
    runtime: static
    region: virginia
    buildCommand: cd frontend && npm install && npm run build
    staticPublishPath: frontend/dist
    envVars:
      - key: VITE_API_URL
        fromService:
          type: web
          name: ai-doubt-backend-v2
          property: host

      - key: VITE_WS_URL
        fromService:
          type: web
          name: ai-doubt-backend-v2
          property: host